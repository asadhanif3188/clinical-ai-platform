[project]
name = "clinical-ai-clinflow"
version = "0.1.0"
description = "ClinFlow AI — Workflow Orchestration Engine"
requires-python = ">=3.12"
dependencies = [
    "clinical-ai-shared",
    "langgraph>=0.1.4",
    "pyyaml>=6.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
