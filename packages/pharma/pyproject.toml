[project]
name = "clinical-ai-pharma"
version = "0.1.0"
description = "PharmaSafe AI — Drug Interaction Checker"
requires-python = ">=3.12"
dependencies = [
    "clinical-ai-shared",
    "clinical-ai-memory",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
