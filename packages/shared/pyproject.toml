[project]
name = "clinical-ai-shared"
version = "0.1.0"
description = "Shared infrastructure and schema package for Clinical AI Platform"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/clinical_ai_shared"]
