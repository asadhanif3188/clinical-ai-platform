[project]
name = "clinical-ai-memory"
version = "0.1.0"
description = "5-Layer Memory System for Clinical AI Platform"
requires-python = ">=3.12"
dependencies = [
    "clinical-ai-shared",
    "rank-bm25>=0.2.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
